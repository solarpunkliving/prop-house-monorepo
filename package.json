{
  "name": "lilrounds",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,sol}\""
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "turbo": "^2.1.0"
  }
}
